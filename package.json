{
  "private": true,
  "name": "todo-list-monorepo",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.54.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "lint": "eslint . --ignore-path .gitignore --ext .ts,.tsx,.js",
    "lint:fix": "yarn lint --fix"
  }
}
